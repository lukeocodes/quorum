# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=quorum

# Encryption Key (REQUIRED - Generate using crypto.randomBytes(32).toString('hex'))
ENCRYPTION_KEY=

# Web App URL (for OAuth redirect)
VITE_WEB_APP_URL=http://localhost:4321

# Development Mode
NODE_ENV=development
