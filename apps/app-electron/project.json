{
  "name": "app-electron",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/app-electron/src",
  "projectType": "application",
  "tags": [
    "npm:private",
    "npm:electron",
    "npm:ai",
    "npm:chat",
    "npm:discussion",
    "type:app",
    "platform:electron"
  ],
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @quorum/app-electron dev",
        "cwd": "{workspaceRoot}"
      },
      "dependsOn": [
        "^build"
      ]
    },
    "dev:vite": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "app-electron:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "app-electron:build:development",
          "hmr": true
        }
      }
    },
    "dev:electron": {
      "executor": "nx:run-commands",
      "options": {
        "command": "wait-on http://localhost:5173 && electron .",
        "cwd": "apps/app-electron"
      }
    },
    "build": {
      "executor": "@nx/vite:build",
      "outputs": [
        "{options.outputPath}"
      ],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "apps/app-electron/dist"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      },
      "dependsOn": [
        "^build"
      ]
    },
    "preview": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vite preview",
        "cwd": "apps/app-electron"
      }
    },
    "db:migrate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/migrate.js",
        "cwd": "apps/app-electron"
      }
    },
    "package": {
      "executor": "nx:run-commands",
      "options": {
        "command": "electron-builder",
        "cwd": "apps/app-electron"
      },
      "dependsOn": [
        "build"
      ]
    }
  }
}
