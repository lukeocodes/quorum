{
  "name": "api-core",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/api-core/src",
  "projectType": "application",
  "tags": [
    "npm:private",
    "npm:express",
    "npm:api",
    "npm:typescript",
    "type:app",
    "platform:node",
    "scope:backend"
  ],
  "targets": {
    "migrate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @quorum/api-core migrate",
        "cwd": "{workspaceRoot}"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @quorum/api-core dev",
        "cwd": "{workspaceRoot}"
      },
      "dependsOn": [
        "^build"
      ]
    },
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/apps/api-core/dist"
      ],
      "options": {
        "command": "pnpm --filter @quorum/api-core build",
        "cwd": "{workspaceRoot}"
      },
      "dependsOn": [
        "^build"
      ]
    },
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @quorum/api-core start",
        "cwd": "{workspaceRoot}"
      },
      "dependsOn": [
        "build"
      ]
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @quorum/api-core lint",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
